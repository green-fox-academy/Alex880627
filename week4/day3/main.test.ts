'use strict';

import {apple} from './apple';

let test = require('tape');

test('add two numbers together', (t) => {
  let apple = new Apple;
  let actual:number = apple.;
  let expected:number = 9;
  t.equal(actual, expected);
  t.end();
})
